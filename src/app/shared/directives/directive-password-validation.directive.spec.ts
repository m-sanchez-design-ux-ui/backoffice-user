import { DirectivePasswordValidationDirective } from './directive-password-validation.directive';

describe('DirectivePasswordValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivePasswordValidationDirective();
    expect(directive).toBeTruthy();
  });
});
