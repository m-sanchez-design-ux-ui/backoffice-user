import { ResizeEventDirective } from './resize-event.directive';

describe('ResizeEventDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeEventDirective();
    expect(directive).toBeTruthy();
  });
});
